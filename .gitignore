# Ambiente virtual Python
.venv/
venv/

# Cache Python
__pycache__/
*.pyc

# Segredos / configuração local (nunca versionar)
.env

# Dados do utilizador (BD de conversas, projetos, uploads) — não é código
data/

# Estado interno do Cline (Focus Chain, etc.) — não é parte do projeto
.clinerules/

# Diagnóstico local de ligação — utilitário da máquina, não versionado
test_smoke.py

# Scripts de teste/demo locais (na raiz do projeto)
test_*.py
demo_*.py

# Roteiro de continuidade entre sessões — local, não versionado
docs/ROADMAP.md

# Editor
.vscode/